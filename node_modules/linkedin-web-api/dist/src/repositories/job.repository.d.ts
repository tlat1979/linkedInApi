import { Client } from '../core/client';
import { JobSearchScroller } from '../scrollers/job-search.scroller';
import { JobSearchFilters } from '../types/job-search-filters';
export declare class JobRepository {
    client: Client;
    constructor({ client }: {
        client: Client;
    });
    searchJob({ skip, limit, filters, keywords, }?: {
        skip?: number;
        limit?: number;
        filters?: JobSearchFilters;
        keywords?: string;
    }): JobSearchScroller;
    private fetchJob;
}
