import { Client } from './client';
export declare class Login {
    private client;
    constructor({ client }: {
        client: Client;
    });
    private setRequestHeaders;
    userPass({ username, password, useCache, proxy, sessions }: {
        username: string;
        password?: string;
        useCache?: boolean;
        proxy?: string;
        sessions?: any;
    }): Promise<Client>;
}
