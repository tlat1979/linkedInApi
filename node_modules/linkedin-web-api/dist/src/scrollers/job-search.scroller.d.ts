import { JobSearchHit } from '../entities/job-search-hit.entity';
import { JobSearchFilters } from '../types/job-search-filters';
import { SkipLimitScroller } from './skip-limit-scroller';
declare type FetchJob = ({ skip, limit, filters, keywords, }: {
    skip?: number;
    limit?: number;
    filters?: JobSearchFilters;
    keywords?: string;
}) => Promise<JobSearchHit[]>;
export declare class JobSearchScroller extends SkipLimitScroller<JobSearchHit> {
    private filters?;
    private keywords?;
    private fetchJob;
    constructor({ fetchJob, skip, limit, filters, keywords, }: {
        fetchJob: FetchJob;
        skip?: number;
        limit?: number;
        filters?: JobSearchFilters;
        keywords?: string;
    });
    fetch(): Promise<any>;
}
export {};
